import java.util.*;

public class C {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int caseCount = in.nextInt();
        in.nextLine();
        for (int i = 0; i < caseCount; ++i) {
            String alpha = in.nextLine().toUpperCase();
            String plain = in.nextLine().toUpperCase();
            String crypt = in.nextLine().toUpperCase();
        }
    }
}
