import java.util.*;

public class B {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int testCount = in.nextInt();
        in.nextLine();
        for (int test = 0; test < testCount; ++test) {

        }
    }

    public static class MySet {
        public ArrayList<MySet> components;
        public MySet(MySet o) {
            this.components = new ArrayList<MySet>(o.components);
        }
        public UnionWith(MySet o) {
            for (MySet s : o) {

            }
        }
        public boolean Equals(MySet o) {
            if (this.components.size() == o.components.size()) {

            }
        }
    }
}
